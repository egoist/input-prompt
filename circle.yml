machine:
  node:
    version: 5

test:
  pre:
    - npm install -g browserify testem
  override:
    - npm run test:ci

general:
  branches:
    ignore:
      - gh-pages # list of branches to ignore
      - /release\/.*/
